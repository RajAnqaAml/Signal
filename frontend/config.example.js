// Copy this file to config.js (gitignored) and fill in your Supabase project values.
// The anon key is safe to expose — Row Level Security on the snapshots table only allows SELECT.
window.SUPABASE_CONFIG = {
    url: "https://YOUR-PROJECT-REF.supabase.co",
    anonKey: "YOUR_ANON_KEY_HERE"
};
